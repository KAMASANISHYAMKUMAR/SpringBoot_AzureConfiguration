spring:
  application:
    name: SpringBoot_Azure_Configuration_App

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  cloud:
    azure:
      keyvault:
        uri:
      storage:
        blob:
          account-name:
